{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json; d=json.load(sys.stdin); print(d.get('tool_input',{}).get('file_path',''))\" | { read -r f; case \"$f\" in */frontend/*.ts|*/frontend/*.tsx) (cd frontend && npx eslint --fix \"$f\") ;; esac; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
